rules_version = '2';

// DEVELOPMENT ONLY - Allow all access for emulator testing
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
